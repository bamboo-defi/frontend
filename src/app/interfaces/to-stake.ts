import {TimeValue} from './time-value';

export interface ToStake {
  total: number;
  toStakeAmmount: number;
  maxToStake: number;
  timeToStake: TimeValue;
  multiplyStake: number;
  totalBambooToStake: number;
}
